{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "makeUp xdebug",
            "type": "php",
            "request": "launch",
            "program": "${workspaceRoot}\\public\\index.php",
            "args": [
                "--path", "/Skincare?json",   // Route (module name) and query. E.g.: /test?json&var1=123&var2=444
                "--method", "GET",   // GET or POST
                "--post-data", "",           // Form data
                "--auth", "0",      // Simulate logged in user ("0"=not logged in (default) or "1"=logged in)
            ],
            "externalConsole": false,
            "port": 9003
        }
    ]
}
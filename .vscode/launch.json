{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "makeUp xdebug",
            "type": "php",
            "request": "launch",
            "program": "${workspaceRoot}\\public\\index.php",
            "args": [
                "--mod", "index",   // module name
                "--task", "",       // task name
                "--render", "",     // Output switch ("html" (default) or "json")
                "--auth", "0",      // Simulate logged in user ("0"=not logged in (default) or "1"=logged in)
                "--uid", "3"
            ],
            "externalConsole": false,
            "port": 9003
        }
    ]
}
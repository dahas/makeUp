# Options +FollowSymlinks
RewriteEngine on
RewriteBase /

# if requested URL is NOT a existing file
RewriteCond %{REQUEST_FILENAME} !-f [OR]
# or if requested URL is NOT a directory
RewriteCond %{REQUEST_FILENAME} !-d

# Modules
RewriteRule ^([^/]*).html$ ?mod=$1 [L,QSA]